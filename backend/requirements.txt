fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator==2.2.0
sqlalchemy==2.0.35
python-dotenv==1.0.1
stripe==11.1.1

# psycopg2-binary is only needed if you later switch from SQLite to
# real Postgres. It failed to install on Windows earlier because it
# needs a C++ compiler — leave it out until you're ready for that step.
